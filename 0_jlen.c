#include "main.h"

/**
 * _jlen - prints the length of the string
 * @j: string pointer
 *
 * Return: Always 0
 */

int _jlen(char *j)
{
int i;
for (i = 0 ; j[i] != 0 ; i++)
{

}
return (i);
}

/**
 * _jlenn - _jlen function
 * @j: character pointer
 *
 * Return: Always 0
 */

int _jlenn(const char *j)
{
int i;
for (i = 0 ; j[i] != 0 ; i++)
{

}
return (i);
}
